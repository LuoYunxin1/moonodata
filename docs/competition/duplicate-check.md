# Duplicate check — MoonOData — 2026-09-17

- Candidate: OData 4.0 resource-path / query-option / JSON / EDM codec.
- osc2026-guide: invoked; MoonCakes research via `moon search`.
- moon search: `odata`, `edmx`, `csdl`, `pyodata`, `olingo` — no modules.
- Local registry index: 2519 packages, 0 OData hits.
- GitHub: `odata moonbit` empty.
- Adjacent: GraphQL (`eisem/moon_graphql`, `Lfan-ke/moongql`) is SDL/executor, not OData ABNF. MoonCheck is generic JSON schema. MoonContract is OpenAPI mocks.
- Rejected alternatives: DXF (gmlewis/step CAD), MCAP (vectie/moonrobo MCAP keyword).
- Decision: proceed. No directly overlapping mature OData codec.
